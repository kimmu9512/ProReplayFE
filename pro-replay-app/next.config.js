/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["ddragon.leagueoflegends.com"],
  },
  // ... any other configurations you might add in the future
};

module.exports = nextConfig;
